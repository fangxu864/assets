<%if(data.length == 0 && data.length == false){%>
    <li>暂无相关通知</li>
<%}else{%>
    <% $.each(data,function(index,value){     %>
        <li><span class="noticeTitle" id="<%=value.id%>" title="<%=value.title%>"><%=value.title%></span><span class="noticeTime"><%=value.create_time%></span></li>
    <% }) %>
<%}%>